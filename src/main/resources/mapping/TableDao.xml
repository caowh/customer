<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cwh.order.customer.dao.TableDao">

    <select id="query" parameterType="java.lang.Long" resultType="cwh.order.customer.model.Table">
        SELECT id,t_name,openid FROM FOOD_TABLE WHERE id = #{id};
    </select>

</mapper>